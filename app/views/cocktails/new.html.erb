<div class="container">

<h1> Create a new cocktail </h1>

<%= simple_form_for(@cocktail) do |c| %>
<%= c.input :name %>
<%= c.input :photo, as: :file %>
<%= c.button :submit, class: "btn-primary" %>
  <% end %>

<%= link_to "Home", root_path %>

</div>
